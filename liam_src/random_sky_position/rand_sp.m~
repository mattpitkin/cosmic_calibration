% Random sky positions

Nr = 10;
ra = 2*pi*rand(Nr,1);
dec = -(pi/2) + acos(2*rand(Nr,1) - 1);
iota = (-pi/2) + acos(2* rand(Nr,1)-1);
psi = pi*(rand(Nr,1));
for j = 1:Nr
    [snr, logZ, nest_samples, post_samples] = inspiral_sd_rand_sp(20, ra(j), dec(j), iota(j), psi(j), j);
end



